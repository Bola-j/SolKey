# SolKey

## Development Email (Mailpit)

Local SMTP catcher for development.

- Start Mailpit: `docker compose up -d mailpit`
- Open inbox UI: http://localhost:8025
- SMTP host/port: `localhost:1025` (already set in appsettings.Development.json)
